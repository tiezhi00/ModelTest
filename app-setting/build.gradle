apply from: '../base.gradle'

android {
    resourcePrefix 'app_setting_'
    if(!isRelease){
        applicationId "com.voyah.cockpit.app_setting"
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
}